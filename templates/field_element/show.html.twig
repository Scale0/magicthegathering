{% extends 'base.html.twig' %}

{% block title %}FieldElement{% endblock %}

{% block body %}
    <h1>FieldElement</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ field_element.id }}</td>
            </tr>
            <tr>
                <th>X</th>
                <td>{{ field_element.x }}</td>
            </tr>
            <tr>
                <th>Y</th>
                <td>{{ field_element.y }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('field_element_index') }}">back to list</a>

    <a href="{{ path('field_element_edit', {'id': field_element.id}) }}">edit</a>

    {{ include('field_element/_delete_form.html.twig') }}
{% endblock %}
