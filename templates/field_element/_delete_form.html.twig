<form method="post" action="{{ path('field_element_delete', {'id': field_element.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ field_element.id) }}">
    <button class="btn">Delete</button>
</form>
